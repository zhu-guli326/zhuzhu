{
  "name": "framelab-hand-frame-composer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html live.html app.js effects.js analytics.js framelab-icon.svg bgm.mp3 demo-original.mp4 demo-inside.mp4 demo-output.mp4 dist/",
    "check": "node --check app.js"
  }
}
