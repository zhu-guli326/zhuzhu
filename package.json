{
  "name": "framelab-hand-frame-composer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html live.html live-v2.html app.js gesture-frame.mjs effects.js analytics.js preview-frame.css progressive-preview.js live-toolbar.css live-toolbar.js live-interactive.css live-interactive.js framelab-icon.svg bgm.mp3 demo-original.mp4 demo-inside.mp4 demo-output.mp4 dist/",
    "check": "node --check app.js && node --check gesture-frame.mjs && node --check progressive-preview.js && node --check live-toolbar.js && node --check live-interactive.js",
    "test": "node --test tests/gesture-frame.test.mjs"
  }
}
